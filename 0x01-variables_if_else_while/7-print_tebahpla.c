#include <stdio.h>

/**
 * main - Print all the lowercase alphabet in reverse case
 *
 * Return: Always 0
 */
int main(void)
{
	char letters;

	for (letter = 'z'; letter => 'a'; letter--)
		putchar(letter);

	putchar('\n');

	return (0);
}
